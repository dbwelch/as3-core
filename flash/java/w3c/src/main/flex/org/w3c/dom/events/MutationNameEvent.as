package org.w3c.dom.events
{
	/**
	* 	This module defines the feature MutationNameEvents 3.0
	* 	and depends on the features MutationEvents 3.0 and Core 3.0.
	* 
	* 	<p><strong>Warning:</strong> The MutationNameEvent interface, introduced
	* 	in an earlier draft of this specification, derives from
	* 	the MutationEvent interface, which is deprecated in this
	* 	specification. Thus, this specification describes the
	* 	mutation name event types for completeness, but
	* 	deprecates their use.</p>
	*/
	public interface MutationNameEvent extends MutationEvent
	{
		
	}
}